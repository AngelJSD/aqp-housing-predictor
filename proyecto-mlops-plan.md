# Proyecto Final: Pipeline MLOps de extremo a extremo

## Objetivo

Demostrar en un video de 10 minutos el ciclo completo de MLOps aplicado a un caso real: desde el feature store hasta el monitoreo de drift y reentrenamiento del modelo en producción, usando un stack híbrido que combina Python (donde el ecosistema ML es más maduro) con TypeScript/Node.js/Next.js (donde aprovecho mi experiencia previa como ingeniero de software).

## Caso de uso

**Predictor de precio de propiedades en Arequipa, Perú** (dataset "Property Listings for 5 South American Countries", subset `pe_properties.csv`, ~124 mil registros, ~12,400 de Arequipa). Regresión: dado distrito, superficie, tipo de propiedad y tipo de operación (venta/alquiler), estimar el precio. Es una herramienta que un usuario común y corriente entiende y usaría directamente: "¿el precio que me piden es razonable?".

**Nota importante sobre el dataset:** los datos son de 2020. En vez de ser una limitación, esto se aprovecha como ventaja narrativa: en lugar de *simular* drift artificialmente, se compara el modelo entrenado con datos de 2020 contra un pequeño conjunto de anuncios reales y actuales de Arequipa (30-50 propiedades recolectadas a mano de un portal inmobiliario). El drift que aparece es real (inflación, cambios en el mercado post-pandemia, tipo de cambio), no inventado — un argumento más honesto y convincente para el jurado.

**Alcance de la limpieza de datos (deliberadamente mínimo, ya que el foco de evaluación es el pipeline, no el modelo):**
- Deduplicar registros.
- Descartar filas sin precio, superficie o distrito (no imputables sin inventar datos).
- Normalizar moneda a una sola unidad (tipo de cambio fijo razonable para 2020).
- Filtrar outliers de precio por percentiles (ej. descartar 1% superior/inferior).
- Imputar el ~5% de lat/long faltante con el centroide promedio del distrito (o prescindir de esta feature si el distrito ya está limpio).
- Modelo: XGBoost o LightGBM con hiperparámetros por defecto, sin tuning extenso — suficiente para la demo.

## Decisión de arquitectura: por qué Python + Node

En vez de forzar todo el pipeline a un solo lenguaje, se divide el trabajo según dónde cada tecnología aporta más:

- **Python:** entrenamiento del modelo, definición y materialización de features en el feature store, generación de reportes de drift. Son tareas donde el ecosistema (scikit-learn, XGBoost, Feast SDK, Evidently) no tiene equivalente maduro en Node.
- **TypeScript / Node.js / Next.js:** la API de inferencia (sirviendo el modelo exportado a ONNX), el consumo del feature store en tiempo real, y el dashboard de monitoreo. Son las piezas con las que interactúa el usuario final y el jurado, y donde mi experiencia previa permite un resultado más pulido.

Este split es en sí mismo parte del argumento técnico del proyecto: no es "usé lo que ya sabía", sino una decisión de arquitectura defendible sobre dónde vive cada responsabilidad del sistema.

## Stack técnico

| Componente | Tecnología |
|---|---|
| Feature store (offline) | Feast + Postgres |
| Feature store (online) | Feast + Redis |
| Catálogo de metadata de features | Tabla `feature_metadata` en Postgres |
| Entrenamiento y tracking | Python + XGBoost/LightGBM + MLflow |
| Registro de modelos | MLflow Model Registry |
| Formato de despliegue del modelo | ONNX |
| API de inferencia | Node.js/Fastify + onnxruntime-node |
| Métricas de servicio | Prometheus (`prom-client` en Node) |
| Detección de drift | Evidently (Python) |
| Dashboard de monitoreo | Next.js + Recharts |
| Orquestación/infraestructura | Docker + Docker Compose (servidor propio) |

## Estructura del repositorio

Monorepo en GitHub — no se justifican repos separados para un proyecto de este tamaño y plazo. Notebooks solo para EDA exploratorio; todo lo que deba correr de forma repetible dentro del pipeline va como script `.py` normal (un notebook no se ejecuta bien desde Docker/CI).

```
mlops-arequipa-housing/
├── README.md
├── docker-compose.yml
├── .gitignore
├── .env.example
├── data/
│   ├── raw/              # gitignored — el CSV de 107MB NO va al repo
│   │   └── README.md     # instrucciones de dónde descargarlo (link a Kaggle)
│   └── processed/        # gitignored, se regenera con los scripts
├── notebooks/
│   └── 01_eda_arequipa.ipynb
├── ml/                              # todo el mundo Python
│   ├── requirements.txt
│   ├── data_prep/clean_arequipa.py
│   ├── training/train.py
│   └── monitoring/drift_report.py
├── feature_repo/                    # carpeta estándar que espera Feast
│   ├── feature_store.yaml
│   └── features.py
├── api/                             # servicio Node/Fastify (serving)
│   ├── package.json
│   ├── src/
│   └── Dockerfile
├── dashboard/                       # Next.js
│   ├── package.json
│   ├── app/
│   └── Dockerfile
└── docs/
    └── proyecto-mlops-plan.md       # este documento
```

**`.gitignore` clave:** nunca subir el CSV de 107MB (Git no está pensado para eso — solo un `data/raw/README.md` con el link de descarga), además de `node_modules/`, `__pycache__/`, `venv/`/`.venv/`, `.env`, y `mlruns/` si los artifacts de MLflow no se centralizan en el servidor.

**Para la narrativa del video:** los mensajes de commit pueden seguir la misma numeración de esta secuencia de tareas (ej. `feat: paso 1 - limpieza y validación de datos Arequipa`), dando al jurado una traza clara del proceso — reforzando el mismo concepto de trazabilidad que se demuestra en el sistema.

## Secuencia de tareas de alto nivel

1. **Validar y limpiar el raw data de Arequipa.** Filtrar el CSV completo de Perú al subset de Arequipa (~10% del total) y verificar su calidad de forma independiente — el 5% de lat/long faltante identificado antes corresponde al dataset completo de Perú, no necesariamente al subset de Arequipa, así que hay que revisar completitud y nulos específicamente sobre este subset. Hacer la limpieza mínima necesaria (deduplicar, descartar filas sin precio/superficie/distrito, normalizar moneda, filtrar outliers). Este raw data limpio es el equivalente a la tabla operacional de anuncios (`listings`), separada conceptualmente de la tabla de features que viene en el siguiente paso.

   **TODO:**
   - [x] Script reproducible de descarga del dataset (`scripts/download_dataset.sh`, vía Kaggle API).
   - [x] Cargar `pe_properties.csv` completo y filtrar al subset de Arequipa (confirmar columna de distrito/ciudad/departamento a usar como filtro). → `l2 == "Arequipa"` (departamento, 25 valores únicos, 0 nulos), 12,164 filas. `l4` = distrito.
   - [x] EDA de completitud/nulos específico del subset de Arequipa (precio, superficie, distrito, lat/long, tipo de propiedad, tipo de operación) — no asumir el 5% del dataset completo de Perú. → `notebooks/01_eda_arequipa.ipynb`. Hallazgo importante: `surface_total` nulo en 38.1% de filas, `surface_covered` en 57.0% (vs. price 2.2%, currency 2.4%, lat/lon 3.8%, l4/distrito 11.0%).
   - [x] **Decisión: definición de "superficie".** Coalescer `surface_total` con fallback a `surface_covered`, luego descartar filas donde ambas sean nulas. Razonamiento (detalle con números en `notebooks/01_eda_arequipa.ipynb`, sección "Decision: how to define superficie"): superficie tiene correlación log-log moderada con precio (~0.39–0.43, similar en ambas columnas) — distrito y tipo de propiedad probablemente explican más varianza, así que no es una feature crítica al punto de justificar perder >1/3 de los datos. Cuando ambas columnas están presentes, la diferencia mediana es 0 — `surface_covered` es un sustituto legítimo, no una cantidad distinta. Coalescer es un superconjunto estricto de exigir solo `surface_total` (mismas filas + ~800–1,200 más), sin downside. Incluso el peor caso deja miles de filas, más que suficiente para el baseline con hiperparámetros por defecto que pide el plan.
   - [x] Deduplicar registros. → `ml/data_prep/clean_arequipa.py`. Dedup por contenido (todas las columnas salvo `id` y fechas de publicación) — el `id` ya es único por sí solo (0 duplicados exactos), pero 743 filas eran el mismo anuncio republicado bajo otro `id`/fecha. Se conserva la ocurrencia más antigua (`created_on`). 12,164 → 11,421 filas.
   - [x] Descartar filas sin precio, superficie (según la definición coalescida arriba) o distrito. → `coalesce_surface` + `drop_incomplete` en `ml/data_prep/clean_arequipa.py`. 11,421 → 6,975 filas (4,446 descartadas).
   - [x] Normalizar moneda a una sola unidad (definir tipo de cambio fijo razonable para 2020). → `normalize_currency` en `ml/data_prep/clean_arequipa.py`. Todo a USD (moneda mayoritaria en los datos y convención del sector inmobiliario en Perú). Tipo de cambio fijo: S/3.5 por USD (promedio anual BCRP 2020 real: S/3.494). Filas con moneda desconocida (14 de 6,975) se descartan — no convertibles. 6,975 → 6,961 filas.
   - [x] Filtrar outliers de precio por percentiles (ej. 1% superior/inferior). → `filter_price_outliers` en `ml/data_prep/clean_arequipa.py`. Percentiles calculados **por `operation_type`**, no globalmente: el precio de venta mediano es ~150x el de alquiler, así que un corte global apenas tocaría la cola de Venta y cortaría de forma incorrecta alquileres baratos pero legítimos (verificado: 1% global = $243, muy por debajo del 1% real de Venta que es $21,945). 6,961 → 6,825 filas (136 descartadas).
   - [x] **Filtrar outliers de precio-por-m² (sanity check, agregado post-hoc).** Hallazgo del paso 3 (validación OoT, feedback externo de un ingeniero ML): 13 filas de Venta y 1 de Alquiler tienen `surface` < 10 m² con precio/m² implausible — hasta $1,074,000/m² (un caso de $2.148M con `surface=2`, casi seguro error de tipeo). → **`filter_surface_sanity`** en `clean_arequipa.py`: descarta filas con `surface < 10`. Se consideró un corte por percentil de precio/m² (igual que `filter_price_outliers`) pero se descartó — hubiera botado ~130 filas, la mayoría legítimas (un lote grande y barato por m² es economía real, no error). Un piso absoluto es más quirúrgico. 6,825 → 6,811 filas. Pipeline completo re-corrido: `listings.parquet` → `features.parquet` → reentrenado. Notebooks 1/2/3 actualizados (detalle en sus items correspondientes).
   - [x] Imputar el lat/long faltante con centroide promedio del distrito (o prescindir de la feature si el subset ya viene limpio en ese campo). → `impute_geo` en `ml/data_prep/clean_arequipa.py`. En la práctica es un no-op sobre este dataset: el ~3.8% de nulos crudos de lat/lon se superpone por completo con filas ya descartadas en pasos previos (0 de 6,811 filas llegan sin lat/lon). Se implementó igual como transformación real (no solo un assert) porque se reusa para el lote manual del paso 10, que es otra fuente y podría no tener esa misma superposición.
   - [x] Guardar el resultado como tabla `listings` limpia en `data/processed/`. → `save_listings` en `ml/data_prep/clean_arequipa.py`, formato Parquet (no CSV — preserva dtypes, lo que ya nos mordió una vez con nulos/strings ambiguos en el CSV crudo). `data/processed/listings.parquet`, 6,811 filas × 27 columnas (originales + `surface` y `price_usd` derivadas). Nota de arquitectura: esta tabla NO es todavía el input de entrenamiento — el paso 2 define features sobre ella, el baseline (paso 3) lee esas features directamente sin Feast, y solo el modelo definitivo (paso 6) lee del feature store real de Feast (paso 5).
   - [x] Documentar decisiones de limpieza (conteos antes/después, filas descartadas por motivo) para trazabilidad. → Conteos antes/después impresos en cada paso de `main()` en `clean_arequipa.py`; razonamiento completo de cada decisión en `notebooks/01_eda_arequipa.ipynb` (con código real que llama las funciones del script, sin duplicar lógica) y resumido en este checklist.
   - [x] Encapsular todo en `ml/data_prep/clean_arequipa.py` como script repetible. → Hecho: `load_raw`, `filter_arequipa`, `deduplicate`, `coalesce_surface`, `drop_incomplete`, `normalize_currency`, `filter_price_outliers`, `impute_geo`, `save_listings`, orquestadas en `main()`.

   **Decisión de diseño: `clean_arequipa.py` como funciones reutilizables, no script monolítico.** Cada regla de limpieza (dedup, drop-nulls, coalescer superficie, normalizar moneda, filtrar outliers) va como función importable, con un bloque `if __name__ == "__main__"` delgado que las aplica sobre el CSV raw. Razón: el paso 10 recolecta un lote nuevo de anuncios reales (2020 vs. hoy) que debe pasar por las *mismas* reglas de transformación antes de poder compararse contra el training set o servirse al modelo — si esa lógica se duplica en vez de reusarse, se arriesga skew entre entrenamiento y "producción" (la comparación de drift dejaría de ser honesta). Las mismas funciones se reusan en el paso 10.

2. **Definir las features y su catálogo de metadata.** Con un EDA rápido, decidir qué columnas del raw data son predictivas para el precio (distrito, superficie, tipo de propiedad/operación) y qué derivadas construir (ej. precio histórico promedio por m² por distrito). Documentar cada feature en una tabla `feature_metadata` en Postgres (nombre, descripción, tipo de dato, columna(s) de origen, transformación aplicada, feature view de Feast asociada, owner, fecha de creación, versión) — este catálogo es independiente del registry interno de Feast y sirve como documentación humano-legible que el dashboard puede consultar directamente.

   **Output de esta tarea (dos artefactos distintos, no uno):**
   1. `data/processed/features.parquet` — los *valores* de las features (listings + columnas derivadas, ej. precio promedio por m² por distrito). Esto es lo que lee el baseline (paso 3) para entrenar.
   2. Catálogo `feature_metadata` — *documentación sobre* esas features (nombre, descripción, columna de origen, transformación, owner, versión...), no valores. Empieza como archivo (ver decisión de arquitectura abajo), es un artefacto separado del #1.

   **TODO:**
   - [x] Notebook `notebooks/02_feature_eda.ipynb` (mismo patrón que el paso 1: exploración autocontenida, con código real que importa y llama funciones de un script, no solo prosa) — EDA rápido sobre `data/processed/listings.parquet` para decidir features.
   - [x] Confirmar features base predictivas: distrito (`l4`), superficie (`surface`), tipo de propiedad (`property_type`), tipo de operación (`operation_type`) — validar relación con `price_usd`. → Confirmado en `notebooks/02_feature_eda.ipynb`: distrito abarca ~$75–$1,133/m² dentro de Venta (15x), tipo de propiedad ~$537 (`Lote`) a ~$1,661/m² (`Oficina`). `operation_type` tiene una brecha de escala enorme (precio de venta mediano ~150x el de alquiler) — cualquier estadística derivada debe calcularse **dentro** de cada `operation_type`, igual que la decisión de outliers del paso 1.
   - [x] **Decisión: features derivadas y manejo de leakage.** Precio histórico promedio por m² por distrito, calculado con **leave-one-out suavizado** (smoothing k=10 hacia la media global del `operation_type`), no promedio simple ni LOO puro. Evidencia real en `notebooks/02_feature_eda.ipynb`: el distrito Mollebaya (Venta) tiene solo 6 anuncios, uno con un outlier de $12,500/m² — el promedio simple de ese anuncio queda 203% inflado por su propio valor (leakage real, no teórico). Mollendo tiene solo 1 anuncio — LOO puro da `NaN` ahí. El suavizado resuelve ambos problemas con una sola fórmula: grupos grandes casi no se mueven de su media LOO, grupos pequeños/únicos se contraen hacia la media global en vez de quedar en `NaN` o dominados por un outlier.
   - [x] **Decisión de arquitectura:** el plan pide la tabla `feature_metadata` "en Postgres", pero Postgres recién se levanta en el paso 4. Mientras tanto vive como CSV (`ml/feature_metadata.csv`) — mapea 1:1 a las columnas de la futura tabla, fácil de cargar con `to_sql`/`COPY` en el paso 4. A diferencia de `data/processed/`, este archivo **sí se trackea en git** — es documentación autorada (owner, versión), no dato derivado regenerable.
   - [x] Definir el esquema de `feature_metadata`: `name, description, dtype, source_columns, transformation, feast_feature_view, owner, created_at, version`.
   - [x] Poblar el catálogo con cada feature decidida. → 5 filas en `ml/feature_metadata.csv`: `district`, `surface`, `property_type`, `operation_type`, `district_avg_price_per_m2`. Todas apuntan a la feature view planeada `arequipa_listings_features` (se crea de verdad recién en el paso 5).
   - [x] Encapsular la construcción de features en un script repetible (ej. `ml/data_prep/build_features.py`), mismo patrón de funciones reutilizables que `clean_arequipa.py`. → Hecho: `load_listings`, `smoothed_district_avg`, `build_features`, `save_features`. Lee `data/processed/listings.parquet` (6,811 filas), escribe `data/processed/features.parquet` (6,811 filas × 7 columnas: `id`, 4 features base, 1 derivada, `price_usd` como target). 0 nulos en todas las columnas.

3. **Entrenar un modelo baseline (sin infraestructura todavía).** Entrenar un modelo rápido (XGBoost/LightGBM con default params) directamente sobre la tabla de features ya definida, sin Feast ni Docker todavía. El objetivo es confirmar que el problema es viable (el modelo aprende algo razonable) antes de invertir tiempo en infraestructura.

   **Caveat conocido, no bloqueante para un baseline:** `district_avg_price_per_m2` se calculó como una sola tabla estática sobre las 6,811 filas juntas (leave-one-out solo excluye la propia fila, no separa train/test). Una fila de test todavía puede reflejar en algo el precio de otras filas de test de su mismo distrito. Aceptable para un baseline exploratorio; si esto importa para el modelo definitivo (paso 6), ahí se recalcula solo con el fold de train.

   **TODO:**
   - [x] Elegir librería (XGBoost o LightGBM) e instalar. → **XGBoost**, decidido sin necesidad de comparación empírica (ambas darían calidad similar en un dataset de 6,811 filas). Dos motivos concretos: export a ONNX más maduro (necesario en el paso 6 para la API de inferencia en Node.js), y soporte nativo de categóricas (`enable_categorical=True`), que alimenta directo la siguiente decisión de encoding. Requirió `brew install libomp` (dependencia de OpenMP en Mac). Documentado en `notebooks/03_baseline_model.ipynb`.
   - [x] Decidir encoding de categóricas (`district`, `property_type`, `operation_type`) — soporte nativo de categóricas vs one-hot/label encoding. → **Categóricas nativas** (`enable_categorical=True`), no one-hot. Comparación real en `notebooks/03_baseline_model.ipynb`: nativo gana en R² (0.836 vs 0.830) y MAE, con 5 columnas en vez de 34 (el one-hot de `district`, ~20 valores, no aporta nada aquí). Hallazgo real durante la prueba: el distrito `Camana` quedó solo en el split de test (no visto en train) — caso genuino de categoría no vista, no solo un artefacto del split. Se mapea a `NaN`, que XGBoost trata como missing y rutea nativamente — mismo manejo necesario en `ml/training/train.py` y en inferencia.
   - [x] **Decisión a explorar con evidencia:** ¿un solo modelo con `operation_type` como feature, o dos modelos separados (Venta/Alquiler)? → **Dos modelos separados.** En `notebooks/03_baseline_model.ipynb`: el modelo único entrenado sobre `price_usd` crudo tiene R² = **-567** en Alquiler (catastrófico, muchísimo peor que predecir la media) — el loss de squared error queda totalmente dominado por la escala de Venta. Entrenando en `log(price_usd)` mejora mucho (R² pasa a 0.34) pero sigue siendo claramente más débil que Venta. Dos modelos separados (target crudo, antes de decidir log): R²=0.78 en Venta (similar al unificado en log — 0.78) y R²=0.38 en Alquiler (vs 0.34) — margen más ajustado que en la primera pasada de este análisis, pero la decisión no depende solo de esta comparación: precio de venta y alquiler mensual son magnitudes económicas distintas (pago único vs. recurrente), y entrenar dos modelos no cuesta nada extra. Además tiene sentido más allá de los números: precio de venta y alquiler mensual son magnitudes económicas distintas (pago único vs. recurrente), no la misma cantidad a otra escala.
   - [x] **Decisión a explorar con evidencia:** ¿entrenar sobre `price_usd` directo o `log(price_usd)`? Decidido por separado para cada modelo (ya que son dos modelos independientes). → **`log(price_usd)` para ambos.** MAE mejora en los dos (Venta $46,624→$41,951, Alquiler $339→$301) — la métrica que más importa dado el enfoque en dólares del producto. R² da una señal mixta: mejora bastante en Alquiler (0.38→0.53) pero baja levemente en Venta (0.78→0.76) — dado que MAE coincide en que log es mejor para ambos y la baja en Venta es chica, se trata como ruido de un solo split aleatorio, no una regresión real. Consistente con la distribución sesgada a la derecha típica de precios inmobiliarios. `ml/training/train.py` entrena ambos modelos en log y aplica `exp()` a las predicciones.
   - [x] Split train/test (definir estrategia: random, estratificado por `operation_type`, etc.). → **Random simple** (`test_size=0.2`, `random_state=42`). Estratificar por `operation_type` ya no aplica (modelos separados). Estratificar por `district` se descartó: `Mollendo` (Venta) y `Camana`/`Characato` (Alquiler) tienen 1 solo anuncio — mecánicamente imposible de estratificar (sklearn exige ≥2 por grupo). El manejo de categorías no vistas (→ `NaN`) ya cubre este caso sin romper nada.
   - [x] Elegir métricas de evaluación (ej. RMSE, MAE, R² — posiblemente en escala log y en escala original). → **R², RMSE, MAE, MAPE**, todas en escala dólar (no log — no interpretable para el jurado). MAPE es la métrica principal (responde directo "¿el precio es razonable?", comparable entre Venta y Alquiler pese a la escala distinta): Venta 15.6%, Alquiler 18.0%. Baseline trivial (predecir la media) da MAPE 117–130% — confirma que el modelo aprende algo real, no solo ruido.
   - [x] Entrenar y registrar métricas del baseline (impresas/loggeadas, no hace falta MLflow todavía — eso es el paso 6). → Tabla final de resultados en `notebooks/03_baseline_model.ipynb`, aplicando todas las decisiones anteriores juntas (encoding nativo, dos modelos, target log, split random, R²/RMSE/MAE/MAPE). Venta: R²=0.7593, MAPE=15.6%. Alquiler: R²=0.5339, MAPE=18.0%.
   - [x] Encapsular en `ml/training/train.py`. → `load_features`, `to_categorical`, `train_operation_model`, `save_model`, orquestadas en `main()`. Guarda cada modelo en `data/processed/models/{venta,alquiler}_xgb.json` (gitignored). El notebook usa el mismo mecanismo de split (`split_operation`: filtrar por `operation_type`, luego split independiente) — las métricas impresas por el script coinciden exactamente con las del notebook, no solo aproximadamente.
   - [x] Confirmar que el modelo aprende algo razonable (mejor que un baseline trivial tipo predecir la media) antes de pasar a infraestructura (paso 4). → Confirmado con margen amplio, no marginal: MAPE ~7.5x menor que el baseline trivial en Venta, ~7.2x menor en Alquiler, R² positivo en ambos. Veredicto documentado en `notebooks/03_baseline_model.ipynb`.

   **Feedback externo (ingeniero ML, revisión post-baseline):** el baseline solo reportaba métricas de test, sin chequeo de overfitting. Tres items agregados al TODO como resultado:
   - [x] Comparar métricas de train vs. test para cada modelo (deben ser parecidas, diferencia de un par de puntos) — detecta overfitting/inestabilidad. Agregar a `notebooks/03_baseline_model.ipynb` y a `ml/training/train.py`. → **Overfitting real, no marginal, confirmado también post-fix.** Alquiler: train R²=0.997 vs test R²=0.534 (gap 0.46). Venta: train R²=0.987 vs test R²=0.759, MAPE 3.8%→15.6%. Ambos muy por fuera del "par de puntos" esperado. `ml/training/train.py` ahora imprime train/test/gap para los dos modelos.
   - [x] Validación Out-of-Time (OoT): entrenar excluyendo febrero 2020 (mes completo, no parcial como marzo 2020), evaluar sobre ese mes retenido. → Hecho en `notebooks/03_baseline_model.ipynb`, re-fiteando `district_avg_price_per_m2` solo con datos pre-febrero (leakage-free). Resultado inicial (con el bug de datos aún presente): catastrófico (Venta R²=-1.52, Alquiler R²=0.28). Tras el fix (`filter_surface_sanity`): Venta R²=-0.08 (ya no peor que la media, pero débil), Alquiler R²=0.43 MAPE=98.9%. Mejora real, pero sigue débil — explicado por el overfitting ya confirmado arriba, no un problema nuevo. Regularización queda como el siguiente lever, ya en "Mejoras adicionales".
   - [x] **Interacción a documentar:** `district_avg_price_per_m2` se calcula sobre el dataset completo antes de cualquier split — esto puede hacer que la brecha train/test se vea artificialmente más chica de lo real. Documentado explícitamente en `notebooks/03_baseline_model.ipynb` junto a los números de overfitting.

   **Hallazgo, no estaba en el alcance original de esta tarea — ya resuelto.** El OoT reveló que 13 filas de Venta y 1 de Alquiler tenían `surface` < 10 m² con precio/m² implausible (hasta $1,074,000/m² — un caso de $2.148M con `surface=2`, casi seguro un error de tipeo). Estas filas sobrevivieron la limpieza del paso 1 porque esa limpieza solo filtraba outliers de *precio*, nunca de precio-por-m². Fix aplicado: `filter_surface_sanity` en el paso 1 (ver ahí el detalle), pipeline completo re-corrido, notebooks 1/2/3 actualizados — el 3 ya no narra la búsqueda del bug, solo confirma el resultado limpio y una nota de verificación (0 filas con `surface < 10` tras el fix).

4. **Preparar la infraestructura base.** Levantar los servicios core en Docker Compose: Postgres (incluyendo la tabla `feature_metadata`), Redis, MLflow server, y el Feast feature server.

   **TODO:**
   - [x] Elegir versiones/imágenes base por servicio. → Verificado con `docker manifest inspect` contra cada imagen (no asumido): Postgres `postgres:16-alpine`, Redis `redis:7-alpine`, MLflow `ghcr.io/mlflow/mlflow:v3.12.0` (imagen oficial del proyecto MLflow en ghcr.io; se probaron tags hasta `v4.0.0` — no existe todavía, así que `v3.12.0` es el más reciente confirmado), Feast feature server: no existe imagen oficial de solo feature-server, así que Dockerfile propio sobre `python:3.12-slim` + `pip install "feast[postgres,redis]==0.65.0"` (extras `postgres`/`redis` confirmados en los metadatos de PyPI del paquete `feast`, versión 0.65.0 es la última publicada).
   - [x] `.env.example` con credenciales/config. → Creado en la raíz. Decisiones: (1) Postgres es **una sola instancia** para toda la app — DB principal `aqp_housing` (`feature_metadata`, y desde el paso 5 el offline store de Feast) + DB separada `mlflow` en la misma instancia, para no mezclar tablas internas de MLflow con las nuestras sin pagar el costo de un segundo contenedor. (2) `MLFLOW_HOST_PORT=5001`, no 5000 — verificado con `lsof` que el puerto 5000 ya está tomado localmente por AirPlay/ControlCenter de macOS. (3) `MLFLOW_BACKEND_STORE_URI` referencia otras variables del mismo `.env` (`${POSTGRES_USER}`, etc.) — verificado con un `docker-compose.yml`/`.env` descartable que Docker Compose v5.0.2 sí expande variables-dentro-de-`.env` (no asumido; versiones viejas de Compose no lo hacían).
   - [x] `docker-compose.yml`: servicio Postgres con volumen persistente + healthcheck. → Creado `docker-compose.yml` (raíz) con servicio `postgres` (imagen `postgres:16-alpine`, volumen nombrado `postgres_data`, healthcheck `pg_isready`, y ya monta `./infra/postgres/init` en `docker-entrypoint-initdb.d` para el próximo ítem). Probado de verdad, no solo `docker compose config`: `docker compose up -d postgres` → healthy en el primer chequeo; `psql` confirma versión y que la DB `aqp_housing` existe (y que `mlflow` correctamente **no** existe todavía — es del próximo ítem); tabla de prueba creada, contenedor reiniciado (`docker compose restart postgres`), tabla seguía presente → el volumen persiste de verdad. Stack bajado (`docker compose down`) al terminar la prueba.
   - [x] `docker-compose.yml`: servicio Redis con volumen persistente + healthcheck. → Servicio `redis` agregado (imagen `redis:7-alpine`, `--appendonly yes` para persistencia AOF — necesario porque este Redis es el online store de Feast, no cache descartable; sin AOF, un restart del contenedor perdería los valores materializados). Volumen nombrado `redis_data`, healthcheck `redis-cli ping`. Probado igual que Postgres: `up -d redis` → healthy en el 4to chequeo; `SET`/`GET` de una clave de prueba, `docker compose restart redis`, clave seguía presente → persistencia real confirmada, no asumida. Stack bajado al terminar.
   - [x] Script de init de Postgres que cree la tabla `feature_metadata`. → `infra/postgres/init/` (montado en `docker-entrypoint-initdb.d`, ya declarado en el servicio Postgres del ítem anterior): `01_create_mlflow_db.sql` (`CREATE DATABASE mlflow` — la creación de esta DB había quedado pendiente de este ítem, ver nota en el ítem de `.env.example`) y `02_create_feature_metadata.sql` (tabla vacía, columnas y tipos según el esquema del paso 2: `name` TEXT PK, `description/dtype/source_columns/transformation/feast_feature_view/owner` TEXT, `created_at` DATE, `version` INTEGER). Probado con volumen fresco (`docker compose down -v` primero — los scripts de init solo corren en un data dir vacío): `\l` confirma `mlflow` y `aqp_housing`, `\d feature_metadata` confirma el esquema, `SELECT count(*)` da 0 filas (carga de datos es el próximo ítem), logs del contenedor sin errores (`CREATE DATABASE`/`CREATE TABLE` limpios).
   - [x] **Decisión: script de carga aparte.** El init de Postgres (`docker-entrypoint-initdb.d`) solo crea el esquema de la tabla `feature_metadata` (vacía). Un script aparte carga `ml/feature_metadata.csv` — separa esquema de datos, más fácil de re-correr si el CSV cambia sin reiniciar el volumen de Postgres. → `ml/data_prep/load_feature_metadata.py` (mismo patrón de funciones reutilizables + `main()` delgado que `clean_arequipa.py`): `load_csv`, `get_connection`, `upsert_feature_metadata` (upsert vía `ON CONFLICT (name) DO UPDATE`, no `INSERT` puro — permite re-correrlo sin duplicar filas). Corre desde el host (no dentro de la red de Compose), así que se conecta a `localhost:${POSTGRES_HOST_PORT}`, no al nombre de servicio `postgres`. Nuevas deps en `ml/requirements.txt`: `psycopg2-binary`, `python-dotenv` (el script carga `.env` directo, no depende de que el shell ya tenga las variables exportadas). Probado con volumen fresco: 1ra corrida → 5 filas cargadas, coinciden con el CSV; 2da corrida (mismo CSV) → sigue en 5 filas, no duplica; prueba de upsert real (no solo ausencia de duplicados): se corrompió manualmente `version` de `district` a 999 vía `UPDATE`, se re-corrió el script, y volvió a 1 — confirma que la rama `DO UPDATE` de verdad se ejecuta.
   - [x] `docker-compose.yml`: servicio MLflow server. → `infra/mlflow/Dockerfile` (extiende `ghcr.io/mlflow/mlflow:v3.12.0` — la imagen oficial no trae driver de Postgres; confirmado con `pip show psycopg2` dentro de la imagen, instala `psycopg2-binary`). Backend store: `MLFLOW_BACKEND_STORE_URI` apuntando a la DB `mlflow` (decidida en el ítem de `.env.example`). Artifacts: **bug real encontrado y corregido antes de dar el ítem por terminado** — usar `--default-artifact-root /mlruns` (plano) hace que el *cliente* de MLflow intente escribir artifacts directo en su propio filesystem en `/mlruns` (asumiendo storage compartido con el servidor); probado con el cliente real corriendo en el host, falló con `OSError: Read-only file system: '/mlruns'` (la raíz de macOS es read-only). Fix: `--artifacts-destination /mlruns` en vez de `--default-artifact-root` — mantiene `--serve-artifacts` (proxy, default `True`), así el cliente sube/baja artifacts vía HTTP al servidor, y el servidor es quien escribe a su propio disco (bind-mounteado a `./mlruns` en el host). Variable renombrada `MLFLOW_ARTIFACT_ROOT` → `MLFLOW_ARTIFACTS_DESTINATION` en `.env.example` para que el nombre coincida con el flag real y no genere confusión a futuro. Probado end-to-end tras el fix, no solo el healthcheck: cliente real (`mlflow` Python package) desde el host — corrida logueada con param/métrica/artifact sin error; archivo del artifact confirmado en `./mlruns/<exp>/<run>/artifacts/` en el host (contenido correcto); fila del run y de la métrica confirmadas con `psql` contra la DB `mlflow`. Datos de prueba limpiados (`docker compose down -v`, `rm -rf ./mlruns`) al terminar.
   - [x] `feature_repo/feature_store.yaml` mínimo: registry, offline store apuntando a Postgres, online store apuntando a Redis — **sin feature views todavía** (eso es el paso 5, no scope-creep aquí). → Creado. `provider: local`, `registry: data/registry.db` (confirmado que `feast apply` crea el directorio `data/` solo si no existe — no hace falta commitear un placeholder). Esquema real de los config objects (`PostgreSQLOfflineStoreConfig`, `RedisOnlineStoreConfig`) introspeccionado directo desde el paquete instalado (`feast==0.65.0`), no adivinado de memoria — evitó dos bugs reales: (1) el default de `sslmode` en el offline store de Postgres es `require`; nuestro Postgres local no tiene SSL configurado, y se confirmó el fallo real (`server does not support SSL, but SSL was required`) antes de fijar `sslmode: disable` explícito. (2) el offline store de Postgres de Feast usa **`psycopg` v3** internamente (no `psycopg2`, el driver que sí usan MLflow y `load_feature_metadata.py`) — mismo comportamiento de `sslmode` a nivel libpq, confirmado igual. Sustitución de variables de entorno (`${POSTGRES_USER}`, etc.) en el YAML confirmada funcional con un repo Feast descartable antes de confiar en ella. Validado con conexiones reales (no solo `feast apply` con éxito, que no toca los stores si no hay feature views): usando las clases de conexión internas de Feast contra el Postgres/Redis ya levantados — `SELECT count(*) FROM feature_metadata` vía la conexión Postgres de Feast, y `PING` vía el cliente Redis de Feast, ambos exitosos. `POSTGRES_HOST`/`POSTGRES_PORT`/`REDIS_HOST`/`REDIS_PORT` (nombres de servicio internos de Compose, no valores de usuario) se definirán en el servicio del feature server del próximo ítem, no en `.env.example` — son topología fija de la red de Compose, no configuración. `feature_repo/data/` agregado a `.gitignore` (regenerable, igual que `data/processed/`).
   - [x] `docker-compose.yml`: servicio Feast feature server — Dockerfile propio, monta `feature_repo/`, expone puerto. → `feature_repo/Dockerfile` (`python:3.12-slim` + `feast[postgres,redis]==0.65.0`) + `feature_repo/entrypoint.sh` (`feast apply` seguido de `exec feast serve --host 0.0.0.0 --port 6566`). Antes de escribir el healthcheck se probó `feast serve` localmente (fuera de Docker) contra Postgres/Redis reales para confirmar rutas reales vía `/openapi.json` — sí existe `/health` (200), y de paso quedaron confirmadas las rutas reales que usarán los pasos 5/10 (`/get-online-features`, `/materialize`, `/materialize-incremental`). Servicio en `docker-compose.yml`: variables `POSTGRES_HOST=postgres`/`REDIS_HOST=redis` (nombres de servicio internos, no de `.env.example` — son topología fija de la red de Compose, no config de usuario), volumen `./feature_repo:/app/feature_repo` (permite iterar en el paso 5 sin rebuild). Probado con el stack completo desde volumen fresco (`docker compose up -d --build`, los 4 servicios juntos): los 4 healthy en el primer chequeo; logs del contenedor Feast confirman `feast apply` limpio contra Postgres/Redis reales (no localhost simulado — la red interna de Compose de verdad); `registry.db` persistido en el host vía el bind mount.
   - [x] **Decisión: `feast apply` vía entrypoint script.** El Dockerfile del feature server usa un entrypoint propio que corre `feast apply` (contra el `feature_repo/` montado) y luego arranca `feast serve` — no init container separado, no paso manual en README. → Implementado en `feature_repo/entrypoint.sh` (ver ítem del servicio Feast arriba); confirmado en los logs del contenedor que corre en orden (`apply` limpio, luego el servidor arranca) en cada `docker compose up`.
   - [x] Levantar los 4 servicios con `docker compose up` y verificar cada uno de forma independiente. → Los 4 chequeos pasaron con el stack completo arriba (`docker compose up -d --build`, volumen fresco, luego `python ml/data_prep/load_feature_metadata.py`): `psql` contra `feature_metadata` → 5 filas; `redis-cli ping` → `PONG`; UI de MLflow (`http://localhost:5001/`) → HTTP 200; salud del feature server de Feast (`http://localhost:6566/health`) → HTTP 200.
   - [x] Nueva sección "Etapa 4" en `README.md` con comandos reproducibles. → Agregada. Cada comando documentado se re-verificó literalmente contra el stack real (no solo escrito de memoria), incluyendo una prueba adicional de arranque en frío completo (`.env` borrado, volúmenes borrados, recreados desde cero) para descartar dependencias de orden ocultas.

5. **Construir el feature store.** Definir las feature views en Feast siguiendo exactamente el diseño ya documentado en el catálogo de metadata. Materializar las features al online store.

   **TODO:**
   - [x] **Decisión: `event_timestamp` = `created_on` real, no sintético.** → Evidencia real antes de decidir: `created_on`/`start_date` en `listings.parquet` tienen 0 nulos ambas, mismo rango (2019-03-01 a 2020-03-24), y son **idénticas en las 6,811 filas** (`end_date` se descartó — tiene un valor centinela `9999-12-31` para anuncios sin fecha de baja). `created_on` además ya es el campo canónico usado en el resto del proyecto (orden de dedup en `clean_arequipa.py`, split OoT del notebook 3) — reusarlo evita introducir un segundo concepto de fecha. `build_features.py` extendido para propagar `created_on` a `features.parquet` (antes 7 columnas, ahora 8). Verificado que no rompe nada downstream: `train.py` selecciona `FEATURE_COLS` explícitamente por nombre (no posicional), así que la columna nueva no se filtra al modelo — se re-corrió `train.py` y las métricas quedaron iguales a las ya documentadas (Venta MAPE 15.7%, Alquiler 17.4%, ambas dentro del ruido ya conocido de un split aleatorio). Notebooks 02/03 no muestran `.columns`/`.head()` de la tabla completa en ninguna celda — no quedaron desactualizados, no hizo falta re-ejecutarlos.
   - [x] Cargar los *valores* de `data/processed/features.parquet` a una tabla real en Postgres (el offline store de Feast lee de SQL, no de Parquet directo) — nueva tabla (ej. `features`), script de carga siguiendo el mismo patrón que `load_feature_metadata.py`. → `infra/postgres/init/03_create_features.sql` (esquema vacío: `id` TEXT PK, `created_on` TIMESTAMP, `district`/`property_type`/`operation_type` TEXT, `surface`/`district_avg_price_per_m2`/`price_usd` DOUBLE PRECISION — `price_usd` sí vive en la tabla aunque no es una de las 5 features del catálogo, porque Feast solo lee las columnas que la `FeatureView` declara; el resto de la tabla no le importa). `ml/data_prep/load_features_to_postgres.py`: a diferencia de `feature_metadata` (upsert por nombre, editado a mano), esta tabla se regenera completa cada vez que corre `build_features.py`, así que el loader hace `TRUNCATE` + insert masivo, no upsert. Conector Postgres compartido extraído a `ml/data_prep/db.py` (antes duplicado idéntico en `load_feature_metadata.py`) para que no diverjan. Probado con volumen fresco: `CREATE TABLE`/`CREATE DATABASE` limpios en los logs de init; carga real → 6,811 filas, muestra verificada con `psql` (fechas parseadas correctamente como `TIMESTAMP`); segunda corrida del loader → sigue en 6,811 filas, confirma que el `TRUNCATE` no duplica.
   - [x] Definir la `Entity` en Feast (ej. `listing`, join key `id` — reusar la columna tal cual, sin rebautizarla, no hace falta la abstracción extra). → `feature_repo/features.py`: `Entity(name="listing", join_keys=["id"], value_type=ValueType.STRING)`. `value_type` explícito tras un `DeprecationWarning` real de `feast apply` (`value_type` será obligatorio en la próxima versión) — `STRING` porque `id` es un hash tipo `"4oBAQqoLR7EEEN85uot6+g=="`, no numérico.
   - [x] Definir el `PostgreSQLSource` apuntando a la tabla del primer ítem, con `timestamp_field` = la columna decidida en el ítem anterior. → `PostgreSQLSource(name="features_source", table="features", timestamp_field="created_on")`. Import real confirmado por introspección del paquete instalado (no está en el namespace top-level `feast`, vive en `feast.infra.offline_stores.contrib.postgres_offline_store.postgres_source`).
   - [x] Definir la `FeatureView` `arequipa_listings_features` en `feature_repo/features.py` — mismo nombre y mismas 5 features que ya están documentadas en `ml/feature_metadata.csv` (`district`, `surface`, `property_type`, `operation_type`, `district_avg_price_per_m2`), sin agregar ni quitar ninguna. Decidir `ttl`. → `ttl=timedelta(days=0)` (vive para siempre): cada fila es el snapshot propio de un listing en su `id`, no un valor que decae por antigüedad (a diferencia de un agregado rolling típico donde TTL sí importa) — y el dataset es chico (6,811 filas), así que la advertencia de la librería sobre TTL=0 siendo costoso en queries grandes no aplica aquí. Se verificó además (leyendo el docstring real de `FeatureView.__init__`, no de memoria) que el parámetro `offline=` de `FeatureView` es sobre *escribir* al offline store (push sources), no sobre poder leer con `get_historical_features` desde un source batch — nuestro caso no lo necesita, default `False` está bien.
   - [x] Confirmar que `feast apply` (ya automático vía el entrypoint del paso 4) recoge `features.py` sin tocar `Dockerfile`/`entrypoint.sh`. → Confirmado con el contenedor real (no solo el CLI local): `docker compose up -d --build`, logs del contenedor Feast muestran `Deploying infrastructure for arequipa_listings_features` sin haber tocado ninguno de los dos archivos — el bind mount de `./feature_repo` ya alcanza.
   - [x] Materializar al online store (Redis). → `feast materialize 2019-01-01T00:00:00 2020-04-01T00:00:00` (rango con margen sobre el real de `created_on`, 2019-03-01 a 2020-03-24), corrido manualmente vía `docker compose exec feast feast -c /app/feature_repo materialize ...` — no automático en el arranque del contenedor (sería lento/redundante en cada restart, mismo criterio que la carga de `feature_metadata.csv`). `redis-cli DBSIZE` → 6,811 — coincide exactamente con las filas de la tabla `features`.
   - [x] Validar de verdad: comparar el valor de un `id` conocido entre `features.parquet` (fuente), `get_historical_features` (offline store) y `get_online_features` vía el feature server real (`/get-online-features`, endpoint ya confirmado en el paso 4) — deben coincidir. → Las tres coinciden exactamente para `id=KJlhh3C7WTxl8gf76ZL82g==` (`district=Socabaya`, `surface=4083.0`, `property_type=Lote`, `operation_type=Venta`, `district_avg_price_per_m2≈729.221874`), incluyendo el `event_timestamp` correcto por feature (`2019-05-15`, el `created_on` real) en la respuesta del feature server. El offline store se consultó con un `event_timestamp` de prueba en 2026 para confirmar que el join point-in-time sigue resolviendo con `ttl=0` (vive para siempre).
   - [x] **Fuera de alcance, no scope-creep:** recalcular `district_avg_price_per_m2` de forma leakage-free por fold de train sigue diferido al paso 6 (ya documentado en el paso 3) — este paso solo materializa el valor estático que ya existe en `features.parquet`. → Confirmado: no se tocó `smoothed_district_avg` ni su lógica.
   - [x] Nueva sección "Etapa 5" en `README.md` con comandos reproducibles. → Agregada. Re-verificada de punta a punta con un arranque en frío completo (`.env` recreado, volúmenes borrados, stack reconstruido) corriendo cada comando documentado tal cual está escrito — no solo escrito de memoria.

6. **Entrenar y registrar el modelo definitivo.** Reentrenar (o reusar el baseline validado en el paso 3) leyendo features desde el offline store de Feast, trackear el experimento en MLflow, y registrar la versión ganadora en el Model Registry. Exportar el modelo final a formato ONNX y validar que las predicciones coinciden con el modelo original.

   **TODO:**
   - [x] Construir el `entity_df` (`id` + `event_timestamp`=`created_on`) y leer `district`/`surface`/`property_type`/`operation_type` desde el offline store de Feast (`get_historical_features`) — confirmar que los valores coinciden exactamente con los que hoy vienen de `features.parquet` para estas 4 features (no derivadas). → Verificado sobre las **6,811 filas completas**, no una muestra: 0 discrepancias en las 4 features contra `features.parquet`.
   - [x] **Decisión: `district_avg_price_per_m2` se recalcula por fold de train, no se lee de Feast para entrenamiento.** El valor estático en Feast (calculado sobre las 6,811 filas juntas, caveat documentado desde el paso 3) tiene leakage real para evaluación/entrenamiento. Se recalcula en el propio script de entrenamiento reusando `smoothed_district_avg` (`build_features.py`) pero fiteado **solo sobre el fold de train** de cada split — mismo criterio que el paso 3 ya había diferido explícitamente a este paso. El valor estático de Feast queda reservado para el momento de *serving* (paso 7): ahí un listing nuevo no es parte de ningún train set, así que no hay leakage y tiene sentido usar el promedio histórico precomputado (para eso existe el online store). Esto no contradice "leer features desde Feast" del plan — se cumple para 4 de las 5 features; la 5ª es un caso ya señalado como sensible a leakage desde el paso 3.
   - [x] Mismo split random 80/20 (`random_state=42`), mismo target `log(price_usd)`, mismos dos modelos por `operation_type` — sin cambios, siguen validados. **`MODEL_PARAMS` sí tuvo que reabrirse** (ver hallazgo abajo): el plan original decía no tocarlo, pero eso fue antes de descubrir que el fix de leakage lo rompía.
   - [x] **Hallazgo real, no un simple "movió la aguja":** el `district_avg_price_per_m2` sin leakage (fit solo en el fold de train, aplicado punto a punto) **rompió el modelo**, no lo mejoró — con los hiperparámetros del paso 3 (`max_depth=5, min_child_weight=3`), Venta pasó de test R²=0.76 a R²=**-0.61** en el mismo split. Verificado que no era un bug (valores de LOO de train y de lookup de test recalculados a mano, coinciden exactamente con la función) ni un fluke de un solo split (5 `random_state`s distintos: rango -1.65 a +0.36, siempre peor que directamente **no tener la feature** en absoluto, que da 0.43–0.59 de forma estable). Causa real: esos hiperparámetros se habían tuneado por CV en el paso 3 **contra la versión con leakage** de la feature (muy informativa porque casi no había fuga real); la versión honesta es una señal más ruidosa (fit solo sobre el fold de train, grupos más chicos), y esos mismos hiperparámetros dejan que el modelo sobreajuste a ese ruido en vez de ignorarlo. Re-corrido el mismo proceso de CV de 5 folds del paso 3, ahora contra la feature honesta — ningún hiperparámetro probado hizo que la feature honesta superara claramente no tenerla (incluso en la única zona estable, `max_depth=1`, el efecto es neutro, no positivo). **Decisión: se elimina `district_avg_price_per_m2` del modelo** — la señal aparente en el paso 3 era la fuga, no algo real más allá de lo que ya aporta `district` (categórica). `MODEL_PARAMS` re-tuneado por el mismo proceso de CV contra el set de 3 features: `max_depth=2, min_child_weight=20` (Venta CV R²=0.51±0.06, Alquiler CV R²=0.34±0.14 — el más estable encontrado). El valor estático en Feast/`feature_metadata.csv` de esta feature no se toca — sigue siendo útil para *serving* (paso 7), donde un listing nuevo nunca fue parte de ningún train set. Todo documentado con código real y ejecutado en `notebooks/04_definitive_model.ipynb`.
   - [x] Re-confirmar métricas train/test/gap sobre el pipeline nuevo (Feast + sin `district_avg_price_per_m2` + hiperparámetros re-tuneados). → Venta: test R²=0.4596 (antes 0.7593), MAPE=41.3% (antes 15.6%), gap R²=0.12 (antes 0.20 — menos overfitting). Alquiler: test R²=0.1689 (antes 0.5339), MAPE=44.3% (antes 18.0%), gap R²=0.31 (antes 0.45). Modelo definitivo es menos preciso que el baseline pero notablemente menos sobreajustado — costo honesto de sacar leakage real, no una regresión a esconder; ambos siguen superando ampliamente el baseline trivial (MAPE 117%/130%). Validación OoT explícitamente **no repetida** en este paso — el ítem la marcaba condicional ("si el tiempo alcanza") y la evidencia de CV ya deja clara la estabilidad del nuevo pipeline; se documenta como omisión consciente, no olvido.
   - [x] Trackear cada corrida en MLflow: un experiment (`arequipa-housing-price`), un run por `operation_type` — parámetros (`MODEL_PARAMS`, transformación log), métricas (R²/RMSE/MAE/MAPE train+test+gap), el modelo como artifact. → `log_to_mlflow` en `train.py`, usando `mlflow.xgboost.log_model` (incluye `registered_model_name`, ver ítem siguiente). Cliente MLflow pineado a `mlflow==3.12.0` en `ml/requirements.txt` — un `pip install mlflow` sin pinear trajo 3.15.0, versión más nueva que el servidor (`v3.12.0`), riesgo real de mismatch cliente/servidor evitado bajándolo a la misma versión. `MLFLOW_TRACKING_URI` agregado a `.env.example` (`http://localhost:${MLFLOW_HOST_PORT}` — el cliente corre en el host). Verificado contra la API REST real del servidor (no solo el log de consola): `GET /runs/get` confirma los 8 params/métricas exactos, coincidiendo con lo impreso en consola.
   - [x] Registrar cada modelo en el Model Registry de MLflow — **dos registered models separados** (`arequipa-price-venta`, `arequipa-price-alquiler`), no dos versiones del mismo nombre. → Mismo `mlflow.xgboost.log_model` con `registered_model_name=f"arequipa-price-{operation_type.lower()}"`. Verificado con `GET /registered-models/search`: dos modelos separados, cada uno con sus propias versiones, `status: READY`. Artifact real confirmado en el host vía el bind mount de `./mlruns` (mismo mecanismo validado en el paso 4). Nota menor real: `mlflow.xgboost.log_model`'s `artifact_path` param está deprecado a favor de `name` — corregido antes de dar el ítem por terminado, no dejado como warning.
   - [x] **Decisión a explorar con evidencia:** ¿el modelo XGBoost con categóricas nativas exporta a ONNX tal cual? → **Sí, funciona directo — la suposición inicial (probablemente necesitaría fallback a one-hot/ordinal) resultó incorrecta.** Probado de verdad con `onnxmltools.convert_xgboost`, no asumido. Dos gotchas reales encontrados y resueltos, ninguno relacionado con categóricas en sí: (1) el conversor exige nombres de feature con el patrón `f%d`, no los nombres de columna reales — falla con `RuntimeError: Unable to interpret 'surface'...` hasta renombrar `booster.feature_names`; hacerlo **sobre una copia** del modelo, no in-place (renombrar in-place rompe la capacidad del modelo original de predecir con sus nombres de columna reales después). (2) las columnas categóricas deben pasarse al runtime ONNX como sus **códigos de categoría de pandas** (enteros, no las etiquetas string) — mismo mecanismo interno que usa XGBoost para sus splits categóricos. Validado con el test set real completo de cada modelo (no una muestra): Venta 956 filas, diff absoluta máxima 8.58e-06; Alquiler 407 filas, diff máxima 3.81e-06 — ambos muy por debajo de la tolerancia elegida (1e-3). Caso de categoría no vista (NaN) probado aparte y también coincide. Sin trade-off de exactitud — no hizo falta el fallback de encoding, así que el precedente de ~0.006 R² del paso 3 (nativo vs one-hot) no aplicó.
   - [x] Exportar a ONNX los dos modelos entrenados. → `data/processed/models/{venta,alquiler}_xgb.onnx` (gitignored, igual que los `.json`).
   - [x] Validar que las predicciones del modelo ONNX coinciden con las del modelo XGBoost original — mismo test set, tolerancia numérica explícita. → Ver ítem anterior — validación real sobre el test set completo de cada modelo, tolerancia `rtol=atol=1e-3` explícita en el código (`np.allclose`), no "a ojo".
   - [x] Encapsular en script(s) repetibles. → **Archivo nuevo**, `ml/training/export_onnx.py` — separa la responsabilidad de exportación/validación del entrenamiento (`train.py`), mismo patrón de funciones reutilizables + `main()` delgado del resto del proyecto. Opera sobre los artifacts `.json` ya guardados en disco (no estado en memoria de `train.py`), reusando `load_training_frame`/`to_categorical`/`FEATURE_COLS` importados de `train.py` para reconstruir el mismo test set exacto — sin duplicar el mecanismo de split. Corrido y confirmado tanto desde `ml/training/` como desde la raíz del repo (`python3 ml/training/export_onnx.py`, la forma documentada en README).
   - [x] Nueva sección "Etapa 6" en `README.md` con comandos reproducibles. → Agregada. Re-verificada con una corrida completa desde volumen fresco (`docker compose down -v`, datos y modelos regenerados desde cero) antes de darla por terminada — deja además el Model Registry en un estado limpio (versión 1 de ambos modelos) en vez del acumulado de corridas de prueba de esta sesión.

7. **Desplegar la API de inferencia.** Construir el servicio de serving en Node.js: recibe requests, consulta features en tiempo real al Feast feature server, corre la inferencia con el modelo ONNX, y devuelve la predicción. Registrar cada request (input, output, latencia, versión de modelo) para trazabilidad.

   **Decisión ya tomada, se aparta de la redacción original de este paso:** el endpoint de predicción **no llama a Feast en tiempo real**. La redacción de arriba se escribió antes del paso 6 — la única feature que hubiera justificado una consulta en vivo (`district_avg_price_per_m2`, para una propiedad nueva sin `id` en el feature store) resultó no aportar nada real más allá de `district` (categórica), y se eliminó del modelo. Mostrarla igual en la UI como "contexto" se descartó explícitamente: es el mismo precio por m² ya reflejado en la predicción, presentado como si fuera una segunda señal independiente — confunde más de lo que aclara, no complementa. El online store de Feast ya quedó demostrado de punta a punta en el paso 5 (materialize → `/get-online-features` → validado contra datos reales); repetirlo acá sin una feature real que lo justifique sería una llamada de puro atrezzo, no una necesidad del producto. `district`/`surface`/`property_type`/`operation_type` los tipea el usuario directo — ninguno requiere lookup para una propiedad que todavía no existe en ningún feature store.

   **TODO:**
   - [x] Exportar el mapeo exacto de categorías (`district`, `property_type` → sus códigos de categoría de pandas, el mismo orden que vio `X_train` en el paso 6) a un artifact que Node pueda leer sin Python. → `save_category_mapping` en `ml/training/export_onnx.py`, `data/processed/models/{venta,alquiler}_categories.json` (uno por modelo — cada `operation_type` tiene su propio split/categorías, pueden diferir). Reusa `X_test`'s dtype directo (ya trae las categorías de train asignadas explícitamente por `to_categorical`, sin necesitar una referencia aparte a `X_train`). Verificado que el índice de una categoría en el JSON coincide exactamente con el código real de pandas (`X_test['district'].cat.codes`) — probado con `Cayma`, índice 2 en ambos.
   - [x] Scaffold del proyecto Node.js/Fastify en `api/` (`package.json`, `src/`, `Dockerfile`) — según la estructura ya sugerida en el plan. → `api/` con `fastify@5.11.0`, `onnxruntime-node@1.27.0`, `pg@8.22.0`, `dotenv` (versiones reales confirmadas en npm, no de memoria), ESM (`"type": "module"`). Nota real de `npm audit`: 2 vulnerabilidades altas en `adm-zip` (dependencia transitiva de `onnxruntime-node`) — confirmado que solo se usa en su script `postinstall` (unzip de binarios prebuilt), no alcanzable desde el código que sirve requests; no se forzó el downgrade que `npm audit fix --force` sugiere (bajaría `onnxruntime-node` a una versión más vieja sin necesidad real).
   - [x] Integrar `onnxruntime-node`: cargar los dos modelos al arrancar, elegir cuál usar según `operation_type`. → `api/src/models.js`. `operation_type` selecciona el modelo (no es un input del modelo en sí — `FEATURE_COLS` en `train.py` no lo incluye).
   - [x] Endpoint `POST /predict`. → `api/src/server.js` + `api/src/encode.js`. Probado end-to-end contra el stack real (Postgres/MLflow ya arriba): `Cayma`/115/`Departamento`/`Venta` → `$122,652.17`, verificado contra `exp(11.717107772827148)` calculado en Python — coincide exacto.
   - [x] **Manejo de categoría no vista**: probado de verdad, no asumido. → Comparado el mismo input (distrito no visto, `NaN`) a través de `onnxruntime` Python y `onnxruntime-node` — predicciones coinciden casi exactas (12.144122 en Node vs 12.144119 en Python), confirma que el runtime de Node enruta `NaN` como missing igual que Python. Probado también vía HTTP real (`POST /predict` con un distrito inventado) — responde normal, sin error, sin caso especial necesario en el handler.
   - [x] **Decisión: tabla `prediction_logs` en Postgres**, no logs de stdout — pensando en el paso 9 (dashboard necesita consultar histórico). → `infra/postgres/init/04_create_prediction_logs.sql` (schema: `operation_type`, `district`, `surface`, `property_type`, `predicted_price_usd`, `model_version`, `latency_ms`, `requested_at`). `api/src/db.js` inserta una fila por predicción exitosa — confirmado que un 400 de validación (falta un campo, `operation_type` inválido) correctamente **no** genera una fila (nunca llega al insert).
   - [x] **Versión de modelo servida**: resuelta contra el Model Registry real. → `api/src/mlflow.js`, `POST /api/2.0/mlflow/registered-models/get-latest-versions` al arrancar, cacheada en memoria (no por request). Confirmado en la respuesta real del endpoint: `model_version: "1"`, coincide con lo registrado en el paso 6.
   - [x] Dockerfile + servicio `api` en `docker-compose.yml`. → `api/Dockerfile`, monta `./data/processed/models:/data/models:ro` (`MODELS_DIR` explícito, no la ruta relativa de fallback que asume estar corriendo desde el repo local). Dos bugs reales encontrados y corregidos antes de dar el ítem por terminado: (1) **`node:22-alpine` no sirve** — `onnxruntime-node` trae un binario nativo prebuilt que necesita el linker dinámico de glibc; probado de verdad (no asumido, dado lo seguido que este tipo de suposición ya falló antes en el proyecto): falla con `ERR_DLOPEN_FAILED`/`ld-linux-aarch64.so.1: No such file or directory` en musl. `node:22-slim` (Debian) sí funciona, confirmado con el mismo test. (2) **Mismo problema de `--allowed-hosts` de MLflow que en el paso 4, pero disparado ahora**: el contenedor `api` recién lo golpeó al intentar resolver la versión del modelo en el arranque — `403 Invalid Host header` porque el Host header (`mlflow:5000`, nombre de servicio interno) no matchea el allowlist default (localhost + IPs privadas). Se había dejado sin tocar en el paso 4 porque nada more llamaba a MLflow desde otro contenedor todavía. Fix: `--allowed-hosts '*'` agregado al comando de `mlflow` en `docker-compose.yml` — aceptable acá porque este puerto nunca se expone más allá de `localhost` y la red interna de Compose (stack de un solo servidor, no un servicio público). Probado end-to-end con el contenedor real: `docker compose up -d --build api` → healthy, predicción real vía el puerto mapeado al host (`$122,652.17` para `Cayma`/115/`Departamento`/`Venta`, idéntico al resultado ya validado fuera de Docker), fila correspondiente confirmada en `prediction_logs` insertada desde el contenedor (conexión interna a Postgres, no `localhost`).
   - [x] Nueva sección "Etapa 7" en `README.md` con comandos reproducibles (build/run del servicio, ejemplo de request real). → Agregada y re-verificada de punta a punta desde volumen fresco: `docker compose up -d --build` con los 5 servicios juntos (confirmado que `api` crashloopea limpio hasta que existen los artifacts de modelo, y se autorecupera solo gracias a `restart: unless-stopped`, sin necesitar un restart manual) → carga de datos (Etapa 5) → entrenamiento + export (Etapa 6) → los tres comandos documentados de esta sección corridos tal cual, mismos resultados exactos que en corridas anteriores.

8. **Instrumentar monitoreo.** Exponer métricas de la API (latencia, throughput, tasa de error) vía Prometheus. Configurar un job periódico en Python con Evidently que compare la distribución de entrenamiento contra el tráfico reciente y genere reportes de drift.

   **Decisiones ya tomadas al armar este checklist, mismo criterio que el resto del proyecto:**
   - **El job "periódico" es un script invocable a mano** (`python3 ml/monitoring/drift_report.py`), no infraestructura nueva de scheduling/cron — mismo patrón que `materialize`, `train.py`, `export_onnx.py` hasta ahora. "Periódico" se demuestra corriéndolo cuando hace falta, no con un contenedor de cron nuevo.
   - **El reporte de Evidently se guarda como HTML** (su salida nativa) en un directorio compartido, no JSON/tabla — el paso 9 lo sirve/enlaza directo desde el dashboard, sin una capa nueva de parseo.
   - **Expectativa explícita, no scope-creep:** en esta etapa `prediction_logs` solo tiene los pocos requests de prueba del paso 7 (misma distribución de 2020, no tráfico real de 2026) — el mecanismo de drift se valida igual con eso, pero el drift real e interesante para el video es explícitamente el lote de anuncios actuales del paso 10. No inventar tráfico falso acá para simular drift antes de tiempo.

   **TODO:**
   - [x] Instrumentar la API Node. → `api/src/metrics.js` (`Registry`, `Histogram` `http_request_duration_seconds`, `Counter` `http_requests_total`, ambos labeleados `method`/`route`/`status_code`), hook `onResponse` en `server.js` usando `reply.elapsedTime` (propiedad real de Fastify 5, confirmada con una prueba mínima antes de usarla) y `request.routeOptions.url` (no `routerPath` — confirmado que ya no existe en Fastify 5, hubiera dado `undefined` en todas las métricas). Endpoint `GET /metrics`. Probado real: `/health` (200), `/predict` válido (200) e inválido (400 por falta de campo) — las tres combinaciones método/ruta/status_code aparecen como series separadas en el output, confirma que la tasa de error es derivable del label sin un contador aparte.
   - [x] Servicio `prometheus` en `docker-compose.yml` + `infra/prometheus/prometheus.yml`. → `prom/prometheus:v3.6.0` (pinneado, confirmado pullable). Healthcheck vía `wget` (busybox, confirmado presente en la imagen — no tiene `curl` ni un shell con python3 como Feast/MLflow). `PROMETHEUS_HOST_PORT` en `.env.example`, volumen nombrado `prometheus_data` para persistencia.
   - [x] Verificar de verdad que Prometheus scrapea el endpoint y las métricas quedan consultables. → Generado tráfico real (3 `POST /predict` exitosos) y consultado `http://localhost:9090/api/v1/query?query=http_requests_total` directamente — la serie `method="POST", route="/predict", status_code="200"` aparece con valor `3`, confirmando el scrape real (no solo que el contenedor está healthy).
   - [x] Confirmar la API real de Evidently instalada (`0.7.21`) antes de escribir código. → Confirmada por introspección real (`inspect.signature`), no de memoria: `Report(metrics=[...])`, `evidently.presets.DataDriftPreset`, `Dataset.from_pandas(df, data_definition=...)`, `DataDefinition(categorical_columns=..., numerical_columns=...)`, `Report.run(current_data=, reference_data=)` → `Snapshot` con `.save_html(path)`. **Bug real de entorno encontrado y resuelto**: `import evidently` fallaba con `ImportError: Blocked import of regex from current working directory for security reasons` — un hook de seguridad nuevo de NLTK (dependencia transitiva de Evidently, CWE-427) que bloquea cualquier import cuyo archivo resuelto viva físicamente bajo el directorio de trabajo actual. Como el `.venv` de este proyecto vive dentro del repo, su propio paquete `regex` da falso positivo. Leído el código fuente de `nltk/inisec.py` para confirmar la causa exacta (no adivinado) — variable de entorno de escape que la propia librería expone (`NLTK_DISABLE_IMPORT_SECURITY=1`), fijada al inicio de `drift_report.py` antes de importar `evidently`.
   - [x] `ml/monitoring/drift_report.py`. → Compara `district`/`surface`/`property_type` de `features` (Postgres, entrenamiento) contra `prediction_logs` (tráfico reciente), **reportes separados por `operation_type`** (mismo patrón de "dos modelos" del resto del proyecto). Probado real: Venta con las 4,777 filas de referencia vs. las 5 filas de `prediction_logs` (llamadas de prueba del paso 7) — reporte HTML de 3.7MB generado, confirmado que contiene las 3 columnas y las secciones "Data Drift"/"Dataset Drift" (no solo que el script no crasheó). Un `RuntimeWarning: invalid value encountered in divide` de numpy explicado, no ignorado a ciegas: las 5 filas de `prediction_logs` son literalmente idénticas (mismo request de prueba repetido), `surface` con varianza cero — caso degenerado esperado dado que todavía no hay tráfico real variado, no un bug del script. Alquiler se salteó limpio (0 filas en `prediction_logs` para ese `operation_type` — el código lo maneja explícitamente, no revienta). De paso, se limpió un warning real de pandas (`pd.read_sql` sobre una conexión psycopg2 cruda no es un uso soportado) reemplazándolo por cursor + `fetchall` manual.
   - [x] Guardar el reporte HTML en un directorio compartido. → `data/processed/drift_reports/{venta,alquiler}_drift.html`, mismo criterio gitignored que el resto de `data/processed/`.
   - [x] Nueva sección "Etapa 8" en `README.md` con comandos reproducibles. → Agregada y re-verificada de punta a punta desde volumen fresco: los 6 servicios juntos (`api`/`prometheus` bloqueados limpio hasta que existen los artifacts, igual que en el paso 7) → pipeline completo (Etapas 5/6) → los comandos de esta sección corridos tal cual. Confirmado además que el reporte de drift maneja bien un caso `n=1` real (una sola fila en `prediction_logs` para Alquiler en esta corrida) sin crashear — solo warnings esperados de numpy por varianza indefinida con una sola muestra.

9. **Construir el dashboard de observabilidad.** Crear un dashboard en Next.js que consuma la API REST de MLflow (versiones y métricas de entrenamiento), las métricas de Prometheus, los reportes de Evidently, y el catálogo `feature_metadata` (sección de documentación de features), presentando todo en un solo lugar.

   **Recomendación pendiente (evaluación de estabilidad del modelo, paso 3):** Venta tiene más varianza real que Alquiler (CV std=0.34 sobre media R²=0.64, vs. Alquiler std=0.12 sobre media 0.66) — sigue siendo un estimador razonable ("¿el precio es razonable?"), no una tasación precisa. Al mostrar predicciones en el dashboard, considerar un rango o señal de confianza para Venta en vez de un número único preciso, para no sobrevender precisión que el modelo no tiene. No bloqueante, evaluar al construir esta tarea.

   **Decisiones ya tomadas al armar este checklist:**
   - **Arquitectura: el dashboard es un proxy server-side para todo.** Todas las llamadas externas (MLflow, Prometheus, Postgres, la API de inferencia) pasan por el backend de Next.js (Server Components / Route Handlers), usando los nombres de servicio internos de Compose — el navegador solo habla con el propio origen del dashboard, nunca directo con otro servicio. Esto evita dos problemas de una sola vez: (a) el gotcha ya conocido de este proyecto (código server-side necesita nombres internos, código en el navegador necesita los puertos mapeados al host — ya nos mordió con Feast/MLflow desde train.py) directamente no aplica si el navegador nunca llama a nada más que al dashboard mismo; (b) evita tener que configurar CORS en la API de inferencia (paso 7), que hoy no lo tiene. El formulario de predicción interactivo también pasa por un Route Handler del dashboard que llama a `http://api:3000/predict` server-side, no directo desde el navegador.
   - **Postgres se consulta directo desde el dashboard** (`feature_metadata`, `prediction_logs`), no a través de la API de inferencia — mismo criterio que ya usan Feast y la API (cada servicio que necesita Postgres se conecta directo, sin indirección innecesaria). MLflow y Prometheus también tienen sus propias REST APIs y se consultan directo, sin pasar por la API de inferencia tampoco — esa API queda enfocada en su trabajo real (servir predicciones), no como gateway general de datos.
   - **Vacío real en la redacción del plan, se llena acá:** el punto de arriba solo lista 4 fuentes de datos (MLflow, Prometheus, Evidently, `feature_metadata`) — no menciona un formulario de predicción interactivo. Pero el guion del video (paso 11) dice explícitamente "un jurado prueba el predictor... desde el dashboard", que no puede cumplirse sin uno. Se agrega como parte de este paso — no es opcional, el guion ya lo exige.
   - **Señal de confianza para Venta: badge/disclaimer cualitativo, no un rango numérico.** Un rango de predicción real (intervalo de confianza) necesitaría regresión por cuantiles o un ensemble — modelado nuevo, fuera de alcance, no existe en ningún punto del pipeline hoy. El plan mismo ya ofrece "un rango **o** una señal de confianza" como alternativas válidas — se elige la señal cualitativa (ej. "Venta: estimación con más variabilidad — úsala como referencia, no como tasación exacta"), usando los números de CV ya calculados en el paso 3, sin trabajo de modelado nuevo.

   **TODO:**
   - [x] Scaffold del proyecto Next.js en `dashboard/`. → `create-next-app@16.2.12` (TypeScript, Tailwind, App Router, sin `src/`), `pg@8.22.0` + `recharts@3.10.1` agregados. React quedó en `19.2.4` (versión que el propio scaffold de Next fija como compatible, no `19.2.8` — no es una discrepancia, es la elección deliberada del scaffold). El scaffold trae su propio `AGENTS.md`/`CLAUDE.md` advirtiendo que Next 16 tiene cambios grandes respecto a versiones más viejas y que hay que leer `node_modules/next/dist/docs/` antes de escribir código — se siguió esa instrucción de verdad (no ignorada): confirmado por lectura real de `route.md` que Route Handlers siguen el patrón `export async function GET/POST(request)`, que `params` en rutas dinámicas es una Promise (desde v15), y por `environment-variables.md` que Next.js carga `.env` nativo (a diferencia de la API en Node/Fastify, no hace falta el paquete `dotenv`) — esto último simplifica la decisión de arquitectura de este paso: como todo el fetching pasa por el servidor, no hace falta ninguna variable `NEXT_PUBLIC_*`, todas quedan server-only. `npm audit`: 3 vulnerabilidades altas (`postcss`/`sharp`, empaquetadas dentro de `next`) — mismo criterio que `adm-zip` en el paso 7: el fix sugerido baja Next 7 versiones mayores (a 9.3.3, inviable), y ninguna de las dos rutas vulnerables aplica al uso real de este dashboard (CSS en build time, procesamiento de imágenes no usado). No se fuerza el downgrade. Confirmado que `npm run dev` levanta y responde `HTTP 200` antes de construir nada encima.
   - [x] Route Handlers / Server Components para cada fuente. → `dashboard/lib/{mlflow,prometheus,db}.ts` (llamados directo desde Server Components, sin Route Handler intermedio — solo el proxy de `/predict` y el servido de HTML de Evidently son Route Handlers reales, porque necesitan ser invocables desde el navegador o devolver contenido no-JSON). Todas las variables de entorno (`POSTGRES_*`, `MLFLOW_URL`, `PROMETHEUS_URL`, `API_URL`, `DRIFT_REPORTS_DIR`) quedan server-only, sin ningún `NEXT_PUBLIC_*` — consistente con la decisión de arquitectura de este paso (todo pasa por el backend de Next.js). Cada fuente probada contra el stack real, no mockeada: MLflow (`/models` reproduce exacto `test_r2=0.4596` Venta / `0.1689` Alquiler, mismos números del paso 6), Prometheus (`/monitoring` refleja tráfico real generado a propósito), Postgres (`/features` refleja las 5 filas reales de `feature_metadata`).
   - [x] Página/sección **Predictor**. → Formulario cliente (`app/predictor/page.tsx`) + `app/api/predict/route.ts` (proxy server-side a `API_URL`, nunca llamado directo desde el navegador). Probado real: mismo resultado exacto (`$122,652.17`) que las pruebas directas a la API en el paso 7, y el passthrough de un 400 de validación confirmado (mismo `message` de Fastify llega intacto al cliente). Señal de confianza cualitativa para Venta/Alquiler con los números de CV del paso 3, texto fijo (no un intervalo numérico — ver razonamiento ya documentado arriba).
   - [x] Página/sección **Modelos**. → `app/models/page.tsx`, `lib/mlflow.ts` (`get-latest-versions` + `runs/get` encadenados). Verificado byte a byte contra los números ya conocidos del paso 6.
   - [x] Página/sección **Monitoreo**. → `app/monitoring/page.tsx` + `RequestsChart.tsx` (Recharts, `"use client"` — Recharts necesita el DOM, no puede ser Server Component). Requests totales y latencia promedio (`http_request_duration_seconds_sum / _count`, una sola query PromQL instantánea) por ruta/método/status_code.
   - [x] Página/sección **Drift**. → `app/drift/page.tsx` (estática — no embebe datos en sí misma) + `app/api/drift/[operation]/route.ts` (lee el HTML del volumen montado en cada request, 404 limpio si `drift_report.py` todavía no generó ese reporte o si el `operation` no es válido). Probado real: `venta` (no generado en esta corrida) → 404; `alquiler` (sí generado) → 200 con el HTML real de Evidently.
   - [x] Página/sección **Catálogo de features**. → `app/features/page.tsx`, tabla completa de `feature_metadata`. **Bug real encontrado con `next build`, no solo `next dev`:** Next prerenderiza esta página en build time por defecto (una query `pg` no participa del heurístico de fetch-cache que Next usa para detectar páginas dinámicas) — confirmado inspeccionando el HTML generado, tenía los valores reales de la DB horneados de forma estática. Fix: `export const dynamic = "force-dynamic"` — confirmado en el output de `next build` que la ruta pasa de `○ Static` a `ƒ Dynamic`. La página de Drift no necesitó el mismo fix porque no embebe datos de la DB directamente, solo enlaces a un Route Handler que ya es dinámico por su cuenta.
   - [x] Dockerfile + servicio `dashboard` en `docker-compose.yml`. → Build multi-stage con `output: "standalone"` (`next.config.ts` — confirmado leyendo la doc real de Next 16, no de memoria), `node:22-alpine` como base (a diferencia de `api/`: probado real que `pg` no tiene el problema de binario nativo de `onnxruntime-node`, alpine sirve bien acá — no se copió la decisión del paso 7 a ciegas). Monta `data/processed/drift_reports/` como volumen (mismo mecanismo que `api`). `DASHBOARD_HOST_PORT=3002` en `.env.example`. **Dos bugs reales de red encontrados y corregidos, ninguno obvio de antemano:** (1) el servidor standalone de Next hace `process.env.HOSTNAME || '0.0.0.0'`, pero Docker **siempre** setea `HOSTNAME` al ID del contenedor — el fallback a `0.0.0.0` nunca se activa dentro de un contenedor, así que el servidor terminaba escuchando solo en la IP interna del contenedor, no en loopback (confirmado con `netstat` real: `172.21.0.8:3000`, no `0.0.0.0:3000`). Fix: `HOSTNAME=0.0.0.0` explícito en el `environment:` del servicio, override del valor que pone Docker. (2) Ya con el bind correcto, el healthcheck seguía fallando — `wget` en esta imagen Alpine resuelve `localhost` a `::1` (IPv6) primero, y el servidor solo escucha IPv4; confirmado que `wget http://127.0.0.1:3000/` sí funciona pero `wget http://localhost:3000/` no, mismo contenedor. Healthcheck cambiado a `127.0.0.1` explícito. Probado real, contenedorizado, las 6 páginas + el proxy de `/predict` + las dos rutas de drift — mismos resultados exactos que las pruebas fuera de Docker.
   - [x] Nueva sección "Etapa 9" en `README.md` con comandos reproducibles. → Agregada y re-verificada de punta a punta desde volumen fresco: los 7 servicios juntos (`api`/`prometheus`/`dashboard` bloqueados limpio hasta que existen los artifacts, mismo comportamiento ya visto en los pasos 7/8) → pipeline completo (Etapas 5/6) → las 6 páginas del dashboard responden 200, el formulario de predicción da el mismo resultado exacto (`$122,652.17` para Cayma/115/Departamento/Venta) que todas las corridas anteriores.

10. **Demostrar uso real y adaptación del sistema.** Recolectar manualmente un pequeño lote de anuncios inmobiliarios reales y actuales de Arequipa, inyectarlos como "tráfico de producción", generar el reporte de drift correspondiente (drift real 2020 vs. hoy, no simulado), y disparar un reentrenamiento que registre una nueva versión del modelo en MLflow, visible en el dashboard.

    **No ejecutado en esta sesión — sin tiempo disponible para la recolección manual real de anuncios.** Documentado abajo como la receta a seguir cuando haya tiempo/datos, con las dos decisiones de diseño ya resueltas (no hace falta re-explorarlas después):

    - **Recolección de datos:** el usuario provee el lote (CSV/planilla/texto pegado) de ~30-50 anuncios reales y actuales (Venta + Alquiler) de un portal inmobiliario de Arequipa — no navegación automatizada. Coincide con la redacción del plan ("recolectados a mano") y evita cualquier tema de términos de servicio de scraping.
    - **Reentrenamiento real, no cosmético:** el lote nuevo se fusiona de verdad con los datos de entrenamiento (`listings`/`features`) y el modelo se reentrena sobre el dataset combinado — la nueva versión en MLflow es genuinamente distinta porque vio mercado de 2026, no solo un número de versión incrementado por correr el mismo script de nuevo sin cambios reales. Coherente con el nombre de la tarea ("adaptación del sistema", no "versionado cosmético") y con el criterio de rigor ya aplicado en el resto del proyecto (ej. paso 6).

    **TODO (receta, pendiente de ejecutar):**
    - [ ] Recibir el lote del usuario (CSV/planilla) — columnas mínimas: distrito, superficie, tipo de propiedad, tipo de operación, precio pedido, fecha (real, 2026).
    - [ ] Adaptar el lote al esquema crudo que esperan las funciones de `ml/data_prep/clean_arequipa.py` (`l2`=Arequipa, `l4`=distrito, `surface_total`/`surface_covered`, `price`, `currency`, `price_period`, `property_type`, `operation_type`, `created_on` con fecha real de 2026) — reusar las funciones tal cual existen, no reimplementar la lógica de limpieza (mismo principio ya establecido desde el paso 1: este es exactamente el caso que motivó diseñar `clean_arequipa.py` como funciones reutilizables en primer lugar).
    - [ ] **Decisión a explorar con evidencia antes de aplicar:** de los pasos de limpieza existentes, `coalesce_surface`/`normalize_currency`/`drop_incomplete`/`impute_geo` aplican tal cual. `filter_price_outliers` y `filter_surface_sanity` están fiteados sobre percentiles/umbrales de la población de entrenamiento (6,811 filas) — recalcular esos percentiles sobre un lote nuevo de solo 30-50 filas daría un umbral distinto y arbitrario. Decidir: ¿reusar los umbrales ya fiteados en el paso 1 (aplicarlos como constantes al lote nuevo), o dejar el lote nuevo sin este filtro dado su tamaño (con el trade-off documentado, no ignorado)?
    - [ ] Inyectar como "tráfico de producción": llamar al endpoint real `POST /predict` de la API de inferencia (paso 7) por cada anuncio del lote — no construir un mecanismo de ingesta aparte, reusa lo que ya existe. Puebla `prediction_logs` con la distribución real de 2026 (que es literalmente el "tráfico reciente" que `drift_report.py` ya espera).
    - [ ] Comparación predicción del modelo vs. precio real pedido — análisis aparte (notebook o script chico), no un cambio al esquema de producción de `prediction_logs` (esa tabla no tiene ni necesita una columna de "precio real", es trazabilidad de servicio, no evaluación). Cuantifica cuánto se equivoca el modelo de 2020 contra el mercado de hoy — argumento central para el video.
    - [ ] Generar el reporte de drift real: correr `ml/monitoring/drift_report.py` sin cambios de código — ya compara `features` (entrenamiento) contra `prediction_logs`, que ahora tendría tráfico real de 2026 en vez de las pruebas sintéticas de los pasos 7/8.
    - [ ] Fusionar el lote limpio con `listings`/`features` en Postgres, re-materializar en Feast (`feast materialize-incremental`, no un materialize de rango completo — solo lo nuevo), re-correr `ml/training/train.py` sin cambios de código — nueva versión en el Model Registry de MLflow.
    - [ ] Confirmar que la nueva versión aparece sola en el dashboard (paso 9, página Modelos) — no debería necesitar cambios ahí, ya lee "la versión más reciente registrada" de forma dinámica vía la API de MLflow.
    - [ ] Nueva sección "Etapa 10" en `README.md` — comandos que **ejecutarían** este flujo cuando haya datos reales disponibles; dejar explícito que es la receta a seguir, no algo ya corrido en esta sesión.

11. **Grabar el video final.** Guion de 10 minutos siguiendo el flujo completo: raw data vs. tabla de features → catálogo de metadata → arquitectura y su justificación → feature store → entrenamiento y registro → API de inferencia → un jurado prueba el predictor con una propiedad real de Arequipa desde el dashboard → drift real detectado en vivo (2020 vs. hoy) → reentrenamiento → nueva versión reflejada en el sistema.

    **TODO:**
    - [x] Escribir el guion. → `guion_video.md` (raíz del repo, mismo criterio que `proyecto-mlops-plan.md` — no `docs/`). Cada beat mapeado a un artefacto real ya construido, con números y hallazgos reales citados (leakage de `district_avg_price_per_m2`, el R²=-0.61 real, el gotcha de ONNX/categóricas), no genérico.
    - [x] Decidir cómo se maneja el hueco del paso 10. → Resuelto: un bloque (7:00–8:30) narra explícitamente que el mecanismo de drift+reentrenamiento está construido y probado, pero la ejecución con datos reales de 2026 quedó como receta pendiente por falta de tiempo — sin inventar un resultado no corrido, consistente con el criterio de honestidad de todo el proyecto.

## Qué demuestra este proyecto

- Diseño y consumo de un feature store (offline/online, point-in-time correctness).
- Tracking de experimentos y versionado de modelos.
- Despliegue de un modelo en un entorno de producción real (servidor propio, Docker).
- Trazabilidad de las predicciones servidas.
- Monitoreo de infraestructura y de datos/modelo (drift).
- Adaptación del sistema ante cambios en el uso real (reentrenamiento disparado por drift).
- Capacidad de integrar un ecosistema ML basado en Python con un stack de producción en TypeScript/Node.js, tomando decisiones de arquitectura justificadas.

## Mejoras adicionales (si hay tiempo)

Fuera del alcance de las 11 tareas core. Evaluar solo después de tener el pipeline completo funcionando end-to-end — no arriesgar el entregable evaluado por perseguir esto primero.

- **Sistema de ingesta real de anuncios nuevos.** En vez de (o además de) el lote manual único del paso 10, un colector que reutilice las mismas funciones de limpieza de `ml/data_prep/clean_arequipa.py` para: (1) escribir los anuncios nuevos ya limpios en la tabla `listings` de Postgres (fuente de verdad / offline), y (2) correr `feast materialize-incremental` para empujar los valores frescos al online store (Redis) — que es lo que efectivamente consulta la API de inferencia en tiempo real durante la demo. El offline store queda listo para el próximo entrenamiento; el online store es lo que se "siente" en vivo. No requiere un scraper automatizado — puede ser tan simple como un script que recibe un CSV nuevo y corre el mismo pipeline.

**Feedback externo (ingeniero ML, revisión post-baseline paso 3):** validado como buenas prácticas pero no urgentes para este proyecto — bajo payoff dado el tamaño actual del feature set (4 features), o explícitamente fuera de alcance ya documentado.

- **Split estratificado por bin de precio** en vez de `train_test_split` simple — binear `price_usd` en cuantiles y estratificar sobre eso, para asegurar que train y test tengan la misma distribución de precio (no solo la misma proporción de filas).
- **Correlación entre features (multicolinealidad).** Descartar variables "repetidas" que aportan lo mismo al target. Bajo payoff aquí: solo 4 features base, ninguna es duplicado obvio por construcción.
- **Análisis univariado tipo Chi² para regresión.** Discretizar el target en bins y medir cuánto cambia cada variable entre bins — equivalente regresión del Chi² que se usa en clasificación. En gran parte ya cubierto cualitativamente por el EDA existente (spread de precio/m² por distrito y por tipo de propiedad).
- **Análisis multivariado con SHAP + modelo "dummy".** Entrenar un modelo con todas las variables candidatas, calcular SHAP values, quedarse solo con las que más aportan antes de entrenar el modelo "de verdad" con tuneo. Útil cuando hay explosión de features (docenas); no es nuestro caso (4 features).
- ~~**Tuneo de hiperparámetros (Optuna).**~~ **Hecho — regularización mínima, no Optuna.** El chequeo de overfitting/OoT del paso 3 sí reveló inestabilidad real, así que se justificó. `max_depth=5, min_child_weight=3` para ambos modelos, adoptado en `ml/training/train.py` (`MODEL_PARAMS`). Documentado con dos callejones sin salida honestos en `notebooks/03_baseline_model.ipynb` ("Regularization"): (1) regularización manual agresiva empeoró todo; (2) tuning sobre un solo split de validación pareció ganar pero colapsó en el split de producción (R² de Alquiler de 0.53 a 0.04) — inestable, no una mejora real. Solo 5-fold CV reveló el problema real: los defaults de Venta tienen R² promedio de **-0.26 (std 2.16)** entre folds — el split único de 80/20 usado en todo el resto del notebook casualmente daba un resultado que se veía bien (R²≈0.75–0.87) y ocultaba esto por completo. La config elegida arregla eso (Venta: 0.64±0.34) y mejora Alquiler de forma más modesta (0.62→0.66, menor varianza) — elegida por estabilidad entre folds, no por el mejor score en un split.
- **Recalcular `district_avg_price_per_m2` de forma leakage-free (solo con el fold de train), como parte del pipeline de entrenamiento en vez de una tabla estática precomputada.** Ya estaba deferido al paso 6 (modelo definitivo sobre el feature store de Feast); este feedback confirma externamente que es la decisión correcta, no la vuelve más urgente hoy.
